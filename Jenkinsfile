pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'pwd'
                sh 'ls -l /home/harsha/Downloads'
            }
        }
    }
}

