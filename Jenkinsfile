pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'cd /home/harsha/Downloads'
                sh 'ls -l'
            }
        }
    }
}

